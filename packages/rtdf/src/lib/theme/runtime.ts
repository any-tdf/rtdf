import {
	ANYTDFTheme as RTDFTheme,
	darkMode,
	generateColorScale,
	getMode,
	getTheme,
	resolveThemeBuiltInIconLibrary,
	switchMode,
	switchTheme,
	themes
} from '@any-tdf/common/theme/runtime';

export { darkMode, generateColorScale, getMode, getTheme, resolveThemeBuiltInIconLibrary, switchMode, switchTheme, RTDFTheme, themes };
export type { PrimaryAndDarkColor, SwitchThemeInput, ThemeConfig, ThemeProps } from '@any-tdf/common/theme/runtime';
